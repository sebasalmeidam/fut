<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
       <% if current_user %>
      	<%= link_to "FUT", players_path, class: "navbar-brand"  %>	
      <% else %>
      	<%= link_to "FUT", unauthenticated_root_path, class: "navbar-brand"  %>	
      <% end %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li class="<%= 'active' if current_page?(list_path) %>"><%= link_to "Buscar Jugadores", list_path %></li>
      </ul>
      
      <ul class="nav navbar-nav navbar-right">
        <% if current_user %>
          <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Opciones <span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><%= link_to "Ver mi perfil", user_path(current_user) %></li>
            <li><%= link_to "Editar mi perfil", edit_user_path(current_user) %></li>
            <li role="separator" class="divider"></li>
            <li><%= link_to "Cerrar Sesión", destroy_user_session_path, method: :delete %></li>
          </ul>
        	</li>
        <% else %>
        	<li class="<%= 'active' if current_page?(registro_path) %>"><%= link_to "Registrarse", registro_path %></li>
        	<li class="<%= 'active' if current_page?(iniciar_sesion_path) %>"><%= link_to "Iniciar Sesión", iniciar_sesion_path %></li>
				<% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
<% if current_user %>
	<div class="text-right" style="padding-right: 15px">
		Usuario actual: <%= current_user.name ? current_user.name : current_user.email %> || (<%= link_to "Cerrar Sesión", destroy_user_session_path, method: :delete %>)
	</div>
<% end %>